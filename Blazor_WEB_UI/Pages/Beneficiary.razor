@page "/Beneficiaries"

<PageTitle>Beneficiaries</PageTitle>
<button class="btn btn-primary" @onclick="IncrementCount">Add Beneficiary</button>

<h1>Beneficiaries</h1>
<table class="table table-bordered">
    <thead>
        <tr>
            <th>Id</th>
            <th>Name_Eng</th>
            <th>Adress_Eng</th>
            <th>SignatoryTitel_Eng | Signatory_Eng</th>
        </tr>
    </thead>
    <tbody>
        @*  @foreach (var beneficiary in Beneficiaries)*@
        @* {*@
        @*<tr>
            <td>@beneficiary.Id</td>
            <td>@beneficiary.Name_Eng</td>
            <td>@beneficiary.Adress_Eng</td>
            <td>@($"{beneficiary.SignatoryTitel_Eng} | {beneficiary.Signatory_Eng}")</td>
        </tr>
        <tr>
            <td>@beneficiary.Id</td>
            <td>@beneficiary.Name_Ukr</td>
            <td>@beneficiary.Adress_Ukr</td>
            <td>@($"{beneficiary.SignatoryTitel_Ukr} | {beneficiary.Signatory_Ukr}")</td>
        </tr>*@
        @*}*@
    </tbody>
</table>




@code {
    private int currentCount = 0;

    private void IncrementCount()
    {
        currentCount++;
    }

   

    //Beneficiary beneficiary = new Beneficiary(
    //  1,
    //      "Bila Tserkva City Counci",
    //      "Білоцерківська міська рада",
    //      "15, Yaroslav Mudryi St., Bila Tserkva, Kyiv oblast, 09100, Ukraine",
    //      "вул. Ярослава Мудрого 15, м. Біла Церква, Київська обл., 09100, Україна",
    //      "Hennadii DYKYI",
    //      "Геннадій ДИКИЙ",
    //      "City Head",
    //      "Міський голова"
    //              );

    //public List<Beneficiary> Beneficiaries = new List<Beneficiary>(); // Initialize the list here


    // Create an instance of BeneficiaryManager and load beneficiaries from file
    //private BeneficiaryManager beneficiaryManager = new BeneficiaryManager("Beneficiary_data.json");

    //// Load beneficiaries in the OnInitializedAsync or any suitable method.
    //protected override async Task OnInitializedAsync()
    //{
    //    // Load beneficiaries from the JSON file using BeneficiaryManager
    //    beneficiaryManager.LoadBeneficiariesFromFile();
    //    Beneficiaries = beneficiaryManager.GetAllBeneficiaries();
}

}
